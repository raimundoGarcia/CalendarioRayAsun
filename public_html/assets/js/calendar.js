/**
 * @description función que al cargar la página por completo, ejecuta la función de carga del calendario
 * @type type
 */
jQuery(document).ready(function () {
    cargaCalendario();

});
//$(window).resize(function () { //esta función recarga todo el calendario en caso de que la resolucion cambie
//    cargaCalendario();
//});

